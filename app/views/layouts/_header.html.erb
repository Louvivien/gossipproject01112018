<header>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/">The Gossip Project</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" href="<%=gossips_path%>">Les Gossips<span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%=new_gossip_path%>">Créer Gossip</a><span class="sr-only">(current)</span>
      </li>



 </ul>
  </div>

<div class="navbar-right">
      <% if @current_user %>
  <span class="nav-item">Connecté: <%= @current_user.name %> </span>
  <%= form_tag "/users/login", method: :delete do %>
    <input type="submit" value="Déconnexion" />
  <% end %>
<% else %>
  <a href="/users/login">S'identifier</a>
<% end %>
</div>

   
</nav>



</header>